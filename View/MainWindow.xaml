<Window x:Class="Budget.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Budget"
        mc:Ignorable="d"
        xmlns:ViewModel="clr-namespace:Budget.ViewModel"
        Title="Program Title" Height="768" Width="1024" MinHeight="768" MinWidth="1024">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <ViewModel:MainViewModel x:Key="MainViewModelDataSource" d:IsDataSource="True"/>
    </Window.Resources>

    <Window.DataContext>
        <Binding Mode="OneWay" Source="{StaticResource MainViewModelDataSource}"/>
    </Window.DataContext>
    <Grid>
        <TabControl>
            <TabItem Header="Login">
                <Grid>
                    <Button Content="Login" Click="Button_Click" Margin="60,157,0,0" HorizontalAlignment="Left" Width="172" Height="64" VerticalAlignment="Top"/>
                    <TextBox x:Name="username" Margin="60,10,785,669"/>
                    <TextBox x:Name="password" Margin="60,45,785,634"/>
                    <TextBox x:Name="email" Margin="60,80,785,599"/>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
